[PHP]
# BCMath is not necessary, but calculations are much faster with it
extension = bcmath.so
extension = json.so
# For unit tests coverage
extension = pcov.so
# For unit tests coverage output
extension = dom.so
